// Dummy test to verify jest setup
describe('Strapi Application Basic Tests', () => {
  it('Should run a successful test case', () => {
    // Tương lai sẽ setup Supertest bootstrap strapi application instance
    expect(true).toBe(true);
  });
});
