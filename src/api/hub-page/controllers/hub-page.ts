/**
 * hub-page controller (Strapi v5)
 * Dùng core controller — public find/findOne với populate đầy đủ sections.
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::hub-page.hub-page');
